services:
  - type: web
    name: grading-system
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PHP_VERSION
        value: 8.2
      - key: DOCUMENT_ROOT
        value: /var/www/html
